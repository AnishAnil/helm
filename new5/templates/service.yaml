apiVersion: v1
kind: Service
metadata:
  name: {{ include "new5.fullname" . }}
  labels:
    {{- include "new5.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new5.selectorLabels" . | nindent 4 }}

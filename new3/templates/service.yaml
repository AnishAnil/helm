apiVersion: v1
kind: Service
metadata:
  name: {{ include "new3.fullname" . }}
  labels:
    {{- include "new3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new3.selectorLabels" . | nindent 4 }}

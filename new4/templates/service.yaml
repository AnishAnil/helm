apiVersion: v1
kind: Service
metadata:
  name: {{ include "new4.fullname" . }}
  labels:
    {{- include "new4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new4.selectorLabels" . | nindent 4 }}
